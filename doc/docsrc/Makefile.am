
regen:
	pdflatex --output-format=dvi asn1c-usage
	pdflatex --output-format=dvi asn1c-usage
	pdflatex --output-format=dvi asn1c-usage
	dvipdf asn1c-usage

EXTRA_DIST = $(srcdir)/*.tex

CLEANFILES = *.*~ *.aux *.dvi *.log *.out *.toc
